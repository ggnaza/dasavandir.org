<!--
Handoff template per CONVENTIONS.md §0 (progressive disclosure — this is a Tier-1 surface) + §6.
The handoff is the CURATED cold-start orientation surface at `.agent-docs/now/handoff.md`.
UPDATE-IN-PLACE (rewritten to current reality each session — NOT write-once like a checkpoint).
For a zero-loss immutable record (dead-ends, rejected alternatives), write a `checkpoints/` sitrep
(§6) and point to it from here — the handoff orients; the checkpoint preserves.
Write at session end / before compaction / when context exceeds ~80%. Does NOT auto-commit.
Delete this comment block + the inline guidance once filled in.
-->

---
provenance: llm-autonomous
template-version: 1.0.0
created: <YYYY-MM-DD>            # LOCAL date (§2)
last-modified: <YYYY-MM-DD>     # keep current — >7d stale = lint warn (now/ row, §1)
work-unit: WU-NNNN
tags: [handoff, now]
---

# Handoff — <YYYY-MM-DD> — <session slug>

## Objective + detour stack
<!-- The MAIN objective and active WU, plus the side-quest stack: each nested detour →
     what spawned it → what it found → resolved/open. So the next session can climb back up. -->

## Current state
<!-- Branch, working-tree shape, what builds/runs, which gates (npm run build / npm run test:e2e /
      / ) are green vs red right now. -->

## Done this session
<!-- Concrete deliverables, each tied to its WU. Reference commits/SHAs (evidence-on-disk, §7.3). -->

## Next actions
<!-- The ordered resume queue, precise enough to act on cold. -->

## Open questions / blockers
<!-- OQ-NNN references (single source is now/open-questions.md — reference, don't duplicate). -->

## Pointers
<!-- Latest checkpoints/ sitrep · relevant ADRs · reference docs. Reference by ID, don't restate. -->
